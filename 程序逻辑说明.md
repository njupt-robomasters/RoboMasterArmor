# Outpost 装甲板程序逻辑说明

本文档对应当前源码版本，用于项目验收和后续维护。工程使用 PlatformIO、Arduino 框架和 ESP32-C3，程序采用单线程 `loop()` 轮询，不使用 FreeRTOS 任务。

## 一、目录结构

```text
outpost/
├─ include/          公共头文件
├─ src/              C++ 实现文件
├─ platformio.ini    PlatformIO 工程配置
└─ .pio/             编译生成目录，不手动修改
```

主要模块如下：

| 模块 | 作用 |
| --- | --- |
| `main` | 初始化模块并按固定顺序运行主循环 |
| `system_state` | 保存系统角色、网络、裁判和主机状态 |
| `button_controller` | 按键边沿、短按和长按判断 |
| `hit_detector` | ADC 击打阈值、重新武装和冷却判断 |
| `hit` | ADC DMA、击打计数、阵营业务和显示状态组合 |
| `led_controller` | WS2812 实际显示 |
| `can` | CAN 收发、主机接管、状态同步和帧分发 |
| `wifi_manager` | Wi-Fi AP/STA 底层模式切换 |
| `provisioning_web` | 手机配网页面内容 |
| `provisioning` | 配网、扫描、连接重试和保存流程 |
| `referee_client` | 裁判系统 UDP 登录、状态和击打上报 |
| `settings` | 配置读写，保存到 NVS 持久化 |
| `config` | 集中管理用户可调参数、硬件引脚、阈值、时间和通信参数 |

`.hpp` 文件放在 `include/`，提供模块接口；`.cpp` 文件放在 `src/`，实现具体功能。

## 二、硬件和固定参数

| 功能 | 引脚或参数 |
| --- | --- |
| 击打传感器 | GPIO0，ADC1 CH0 |
| CAN RX | GPIO6 |
| CAN TX | GPIO7 |
| 按键 | GPIO9，内部上拉，按下为低电平 |
| WS2812 | GPIO10，22 颗 |
| CAN 速率 | 1 Mbit/s |
| 长按时间 | 1000ms |
| 击打阈值 | ADC 大于 2000 |
| 击打重新武装 | ADC 小于 1000 |
| 击打冷却 | 两次有效击打间隔严格大于 50ms |
| 击打灭灯 | 50ms |
| LED 亮度 | `config.hpp` 中的 `LED_BRIGHTNESS`，范围 0~255，默认 255 |
| 串口 | ESP32-C3 原生 USB CDC，115200 |

### 用户常用修改位置

以后修改下面这些内容，只需要打开 `include/config.hpp` 顶部的“用户可调参数”区域：

```text
HIT_THRESHOLD                 击打阈值
HIT_COOLDOWN_MS               击打冷却时间
LED_BRIGHTNESS                WS2812 亮度，0~255
PROVISIONING_WIFI_PREFIX      手机配网热点名称前缀
PROVISIONING_WIFI_PASSWORD    手机配网热点密码
```

当前默认值：

```text
击打阈值：2000
击打冷却：50ms，实际要求间隔严格大于50ms
LED亮度：255
配网热点前缀：RM_
配网热点密码：12345678
```

## 三、上电流程

`main.cpp` 的初始化顺序如下：

1. 初始化 `SystemState`。
2. 初始化配置存储 `Settings`（从 NVS 读取已保存配置）。
3. 启动 USB CDC 串口和启动诊断输出。
4. 初始化按键。
5. 初始化击打 ADC DMA 和 WS2812。
6. 初始化 CAN/TWAI。
7. 进入普通离线状态，关闭 Wi-Fi。
8. 初始化裁判客户端。

Wi-Fi 名称、密码、Robot ID 和阵营保存在 NVS 中，掉电不丢失。已配网过的板子上电会自动恢复联网，未配网过的板子停在离线状态等待长按配网。

网页默认预填：

```text
Wi-Fi：Potential-Robot
密码：12345678
```

默认值只用于网页快速填写，不代表设备上电自动联网。

## 四、主循环顺序

`loop()` 每次按以下顺序运行：

```text
按键处理
  -> ADC/击打和灯光处理
  -> CAN 收发和主从业务
  -> 配网/Wi-Fi 状态处理
  -> 裁判系统 UDP 处理
```

各模块通过 `SystemState` 读取系统快照。当前工程已经拆分了底层通信、主机管理、状态同步和击打判定，但部分业务状态仍由配网、CAN 和裁判模块协同更新。

## 五、按键逻辑

### 普通状态

- 按下后持续 1 秒，触发长按，进入配网模式。
- 短按在松开后触发，切换红蓝阵营。
- 已登录并在线时，任意一块板长按都可以让该板成为配网主机。

### 配网状态

- 配网状态下，检测到按下沿立即退出配网。
- 这次按键会被锁存，不会因为继续按住 1 秒而再次触发长按。
- 松开后不会再次触发普通短按。

### 阵营切换

- 主机离线时，本板直接切换阵营。
- 在线主机短按时，主机直接切换阵营并重新登录裁判系统。
- 在线从机短按时，通过 CAN 请求主机切换，主机切换后广播新的公共阵营状态。
- 红方 Robot ID 和蓝方 Robot ID 通过加减 100 对应，例如 `3 <-> 103`。

## 六、系统状态

`system_state` 保存以下三类状态：

### 设备角色

```text
Standalone：没有当前主机，独立运行
Host：本板是当前主机
Node：本板跟随其他主机
```

### 网络状态

```text
Offline：离线
Provisioning：配网
Connecting：连接 Wi-Fi
Connected：Wi-Fi 已连接
```

### 裁判状态

```text
Disabled：未启用
LoginPending：等待登录
LoggedIn：登录成功并处于比赛状态
Dead：裁判系统同步到当前血量为 0
```

主要状态流程：

```text
Offline
  长按1秒 -> Provisioning

Provisioning
  按下按键 -> Offline
  网页保存 -> Connecting

Connecting
  Wi-Fi连接成功 -> Connected + LoginPending

LoginPending
  裁判登录并收到比赛同步 -> LoggedIn
  长按1秒 -> Provisioning

LoggedIn
  长按1秒 -> Provisioning
  裁判系统掉线 -> 重新登录流程
  HP为0 -> Dead

Dead
  HP恢复 -> LoggedIn
```

击打不会改变系统网络或裁判状态，只产生本地击打计数和后续上报事件。

## 七、灯光逻辑

灯光由 `led_controller` 输出，状态组合在 `hit.cpp` 中完成。亮度通过 `config.hpp` 的 `LED_BRIGHTNESS` 统一设置，所有灯光状态使用同一个亮度配置。

优先级大致如下：

1. 配网：黄灯闪烁（周期由 PROVISIONING_BLINK_MS 决定），主机和从机同步闪烁。
2. 保存后连接 Wi-Fi：三块板统一黄灯常亮。
3. 裁判登录等待：主机和从机显示登录状态灯光。
4. 阵亡：灯灭。
5. 击打：检测到本板击打后，本板灯灭 50ms，再恢复原状态。
6. 正常在线或离线：显示当前红色或蓝色阵营。

击打灯效是本地效果。一块板被击打时，其他板不会因为这次本地击打而灭灯；击打计数会通过 CAN 传给主机，再由主机向裁判系统逐次上报。

## 八、击打检测

`hit` 使用 ADC 数字控制器和 DMA 采样，采样频率为 10kHz，不使用普通 `analogRead()` 轮询。

判定规则：

1. ADC 数据大于 2000，且当前已经武装，才可能产生击打。
2. ADC 数据降到 1000 以下后重新武装。
3. 两次有效击打间隔必须严格大于 500ms。
4. 有效击打后累计 `hit_count` 加一。
5. `hit_count` 掉电清零，不保存到 Flash。

配网和 STA 扫描/连接期间会停止 ADC DMA，避免 ESP32-C3 的 Wi-Fi DMA 与 ADC DMA 竞争。连接成功后恢复 ADC DMA。ADC 读取异常时会尝试重新启动 DMA。

## 九、配网和 Wi-Fi 流程

### 进入配网

长按 1 秒后：

1. 当前板成为配网主机。
2. 系统状态进入 `Provisioning`。
3. 停止裁判 UDP。
4. 停止 ADC DMA。
5. 启动 SoftAP、DNS 和网页服务器。
6. 三块板通过 CAN 同步为黄灯闪烁。

热点参数：

```text
名称：RM_ + 设备 MAC 地址
密码：12345678
IP：192.168.4.1
信道：6
```

手机连接热点后打开：

```text
http://192.168.4.1/
```

### 保存配置

网页提交后：

1. 校验 Wi-Fi 名称、密码和 Robot ID。
2. 配置写入 NVS，掉电保留。
3. HTTP 返回 `204 No Content`，不显示保存成功页面。
4. 关闭 DNS、网页服务器和 SoftAP。
5. 三块板保持黄灯常亮，避免配网闪烁灭相位残留。
6. 开始 STA Wi-Fi 连接。
7. 不调用 `ESP.restart()`，不会自动重启。

### STA 连接

连接流程由 `provisioning` 负责策略，`wifi_manager` 负责底层 AP/STA 模式切换：

- 连接前关闭自动重连，避免连接和扫描同时进行。
- 扫描目标 SSID，优先选择 2.4GHz 网络和信号较好的 BSSID。
- 初始 STA 协议使用 11g/11n；多次失败后增加 11b 兼容模式。
- 连接失败时大约每 15 秒重新扫描并尝试连接。
- 连接成功后恢复 ADC DMA，并启动裁判系统登录流程。

ESP32-C3 只支持 2.4GHz，因此目标热点必须提供 2.4GHz 网络。

## 十、三块板主从逻辑

三块板烧录同一份固件。完成本次配网并保存 Wi-Fi 的板为主机，另外两块板为从机。

- 主机连接 Wi-Fi 和裁判系统。
- 从机不连接 Wi-Fi，只通过 CAN 跟随主机。
- 主机超过 1.5 秒没有收到有效状态时（配网/联网阶段放宽到 6 秒），从机清除主机并回到离线状态。
- 在线状态下任意一块板长按，该板可以抢占成为配网主机，原主机让出主机身份。

主机每 50ms 广播公共状态，从机同步：

- 配网状态；
- Wi-Fi 连接状态；
- 裁判登录状态；
- 在线状态；
- 阵营颜色；
- 黄灯闪烁相位；
- 阵亡状态。

## 十一、CAN 协议

CAN 相关底层收发、主机管理、状态同步和业务命令现在统一放在 `can.cpp` 中，公共接口只有 `can.hpp`，减少验收和维护时的文件跳转。

当前所有内部 CAN 报文统一为标准 ID `0x000`，固定 DLC=8，且只接受 `data[0]=0xA0` 的报文。不再通过 CAN ID 区分报文类型，也不再使用旧的 `0x123`、`0x180`、`0x181`、`0x182`、`0x183`、`0x184`。

统一报文格式：

```text
byte 0：固定 0xA0
byte 1：模式，0离线、1配网、2连接Wi-Fi、3裁判登录中、4在线、5阵亡
byte 2：标志位 bit0配网、bit1黄灯亮相、bit2发送者为主机、bit3状态变化、bit4主机接管
byte 3：阵营，0为红、1为蓝
byte 4：LED 亮度
byte 5：事件，0状态、1切换阵营、2主机接管、3退出、4击打、5确认
byte 6：状态版本号 uint8_t
byte 7：事件参数或保留
```

主机是唯一的周期状态广播者，从机只接收并保存主机最后一个有效状态。状态版本使用半范围比较处理 `255 -> 0` 回绕，只有新版本覆盖旧版本。按键请求和击打事件由从机设置 pending 标志，在非阻塞请求窗口重试；发送失败不清除 pending，主机处理后用新状态或确认事件清除。

击打事件只表达“存在一个未处理击打”，不携带 MAC、板号或累计计数。主机收到一次有效事件最多向裁判系统上报一次，即使本地计数跳变也不会放大上报次数。三块板使用完全相同固件且不携带身份信息，因此两个板完全同时发起主机接管时无法在协议层可靠区分，正常操作要求同一时刻只有一块板长按。

## 十二、裁判系统 UDP

裁判客户端只在主机 STA 连接成功后工作。

当前配置：

```text
裁判服务器：config.hpp 中的固定 IP
服务器端口：62101
本地端口：60000
```

主要流程：

1. 每约 500ms 发送登录包，直到收到成功响应。
2. 等待比赛状态同步。
3. 比赛中周期发送状态保活包。
4. 本地主机击打或从机转发击打时，发送受击上报。
5. 受击上报使用严格大于 500ms 的发送冷却。
6. 超过约 3 秒没有收到比赛状态时，重新进入登录流程。
7. 收到血量为 0 且最大血量大于 0 时，系统进入阵亡显示；血量恢复后回到在线显示。

## 十三、配置持久化

配置通过 Preferences 保存到 NVS，掉电不丢失。

持久化的内容：

- Wi-Fi 名称和密码；
- Robot ID；
- 阵营颜色；
- 是否完成过一次配网保存的标志。

不持久化的内容：

- 击打计数；
- 当前主从和网络状态。

上电恢复规则：

```text
有已保存配置 -> 自动按已保存的 Wi-Fi 和阵营进入联网流程
无已保存配置 -> 停在离线状态，长按进入配网
```

因此配好网之后不需要每次上电都重新配网。要更换网络或阵营，长按进入配网重新保存即可。

源码没有使用 ESP.restart() 配网重启流程，配网保存后直接在当前运行中切换 STA 连接。

## 十四、维护边界

当前工程已经将公共头文件放入 `include/`，实现文件放入 `src/`，并拆分了按键、击打判定、灯光、CAN、Wi-Fi 底层、配网网页和裁判通信。

目前仍有两个业务协调中心：

- `provisioning.cpp` 仍负责 Wi-Fi 扫描、连接重试和配网业务流程；
- `can.cpp` 仍负责多种 CAN 业务命令和主循环调度。

`SystemState` 当前是统一状态数据中心，但还不是完整的事件驱动状态机。后续如果增加复杂联网恢复、更多系统状态或更多按键动作，建议增加统一事件控制器，让按键、Wi-Fi、CAN、裁判和击打模块只报告事件，由一个控制器决定状态转换。

当前固件的核心设计原则是：

```text
底层模块负责硬件
业务模块负责本领域逻辑
SystemState 保存系统快照
CAN 负责三块板之间的状态和数据同步
所有运行时配置保存到 NVS，掉电保留
```